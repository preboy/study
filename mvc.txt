bean		_instance(Object), _id(string)
protobean	_classType(Class)

BeanProvider  beans(Array)

BeanFactory 
	starlingMVC
	EventMap	

Beans 		
	commands(Directory)		is command
	beans(Directory)		bean     not command

Command
	_event(string)
	_command(Class)
	_oncetime(boolean)


Commands 处理Beans中的command,对事件进行处理，new对象，

BeanEvent(Event) _bean, 

EventMap 
	map ([{target, dispatch, callback}])


StarlingMVC
	dispatcher
	beans:Beans
	initialized
	_config:StarlingMVCConfig;
	_rootLayer:DisplayObjectContainer;
	_processors:Processors;
	beanFactory:BeanFactory;
	_bindings:Bindings 
	_commands:Commands;



	



